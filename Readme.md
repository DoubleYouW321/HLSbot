# 🧘 Чат-бот «Я проектирую свое благополучие»

### Проект для хакатона «Код здорового человека» (Трек: Чат-бот)

---

## 1. Описание проекта

Чат-бот «Я проектирую свое благополучие» — это цифровой наставник, помогающий пользователю осознанно работать над балансом трех ключевых сфер жизни:

*   **Физическое благополучие:** отслеживание показателей (вода, сон, шаги), сравнение с нормой, получение советов по улучшению здоровья.
*   **Психологическое благополучие:** SOS-кнопка с дыхательными упражнениями, дневник настроения, навигатор помощи для обсуждения проблем.
*   **Социальное благополучие:** таймер Pomodoro для продуктивности, мини-тесты для самопознания, гайды по развитию soft skills.

Бот не просто выдает информацию, а вовлекает пользователя через интерактивный диалог и персонализированные инструменты.

**Стек технологий:**
*   **Backend:** Python 3.10+
*   **Фреймворк для бота:** Aiogram 3.x (асинхронный)
*   **База данных:** SQLite 3 + SQLAlchemy 2.0 (асинхронный ORM)
*   **Управление зависимостями:** pip (requirements.txt)
*   **Контроль версий:** Git, GitHub
*   **Проектирование интерфейса и сценариев:** Figma

---

## 2. Инструкция по установке

### Предварительные требования
1.  Установите Python 3.10 или новее.
2.  Установите Git.

### Шаги установки

#### 1. Клонирование репозитория
Откройте терминал и выполните:
```bash
git clone <https://github.com/DoubleYouW321/HLSbot>
cd <название_папки_проекта>
```

#### 2. Создание и активация виртуального окружения
```bash
python -m venv venv
# Активация виртуального окружения:
# На Windows:
venv\Scripts\activate
# На macOS/Linux:
source venv/bin/activate
```

#### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

#### 4. Настройка переменных окружения
1.  Создайте файл `config.py` в корне проекта.
2.  Добавьте в него ваш токен бота, полученный от [@BotFather](https://t.me/BotFather) и chat id администратора:
```env
BOT_TOKEN=ваш_токен_бота_здесь
ADMIN_CHAT_ID=чат_айди_админа
```

---

## 3. Инструкция по запуску

### Стандартный запуск (рекомендуется)
1.  Убедитесь, что виртуальное окружение активировано.
2.  Запустите бота командой:
```bash
python run.py
```
*Примечание: Если файл запуска имеет другое имя, используйте соответствующую команду, например `python main.py`.*

**Проверка:** Перейдите в Telegram и найдите вашего бота по имени. Отправьте команду `/start`.

### О контейнеризации (Docker)
Проект в текущей версии не включает Docker-конфигурацию. Для развертывания в production рекомендуется:
1.  Создать `Dockerfile` и `docker-compose.yml`
2.  Перенести переменные окружения в соответствующие секции
3.  Использовать более производительную БД (например, PostgreSQL)

---

## 4. Архитектура

### Логика работы и функциональные модули
1.  **Начало работы:** Пользователь отправляет `/start` → бот регистрирует его и показывает главное меню с тремя разделами.
2.  **Интерактивное меню:** Через Inline-клавиатуры (Aiogram) доступны:
    
    **A. Физическое благополучие:**
    *   *Получить совет* — персонализированные рекомендации
    *   *Отследить показатели* — ввод воды/сна/шагов
    *   *Сравнить с нормой* — анализ прогресса относительно целей
    
    **B. Психологическое благополучие:**
    *   *SOS-кнопка* — дыхательные упражнения для экстренной помощи
    *   *Дневник настроения* — ежедневное отслеживание эмоций
    *   *Навигатор помощи* — ресурсы для психологической поддержки
    
    **C. Социальное благополучие:**
    *   *Таймер Pomodoro* — управление временем по технике 25/5
    *   *Mini-тесты* — инструменты для самопознания
    *   *Гайды по soft skills* — материалы для развития навыков
    
    **D. Обратная связь:** Команда `/feedback` открывает форму для отзывов.

3.  **Работа с данными:** Все действия пользователя асинхронно сохраняются в БД через SQLAlchemy ORM.
4.  **Состояния (FSM):** Для многошаговых сценариев используется машина состояний Aiogram.

### Структура базы данных (SQLite)
Проект использует 5 основных таблиц:

1.  **`users`** — Основная таблица пользователей.
    *   `id` (PK), `tg_id` (уникальный ID из Telegram).

2.  **`daily_metrics`** — Ежедневные показатели физической активности.
    *   `id` (PK), `user_id` (FK → users.id), `date`, `water_glasses`, `sleep_hours`, `steps`.
    *   Уникальное ограничение `(user_id, date)` для одной записи в день.

3.  **`categories`** — Персональные цели пользователя (нормы).
    *   `id` (PK), `user_id` (FK → users.id), `water` (норма стаканов), `hours` (норма сна), `steps` (норма шагов).

4.  **`mood_records`** — Записи настроения для психологического раздела.
    *   `id` (PK), `user_id` (FK → users.id), `date`, `mood` (текстовое описание), `emoji` (соответствующий эмодзи).

5.  **`feedback`** — Отзывы и предложения от пользователей.
    *   `id` (PK), `tg_id` (ID пользователя), `comment_text` (текст отзыва).

**Связи:**
*   Один пользователь (`User`) имеет **одну** персональную настройку целей (`Category`).
*   Один пользователь (`User`) может иметь **много** ежедневных записей (`DailyMetric`) и записей настроения (`MoodRecord`).

---

**Связь с концепцией хакатона:** Бот полностью реализует целостный подход к благополучию, предложенный кафедрой философии СИБГМУ, охватывая физический, психологический и социальный аспекты через практические инструменты и интерактивные сценарии.